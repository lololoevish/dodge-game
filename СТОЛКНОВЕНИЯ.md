# ⚡ СИСТЕМА СТОЛКНОВЕНИЙ МЕЖДУ ФИГУРАМИ

## Описание
Добавлена система столкновений между игровыми фигурами с реалистичным отскоком и изменением направления движения.

## Как работает

### 1. Проверка столкновений
Каждый кадр игра проверяет столкновения между всеми парами фигур:
- Вычисляется расстояние между центрами фигур
- Если расстояние меньше суммы радиусов - происходит столкновение

### 2. Исключения
Не участвуют в столкновениях:
- ⭐ **Звездочки** - статичные генераторы
- 💣 **Мины** - взрывные устройства
- 📏 **Лазеры** - линейные объекты

## Поведение при столкновении

### 🟢 Зеленый отскакивающий круг
- **Эффект**: Отскакивает в противоположном направлении
- **Механика**: Меняет вектор скорости на противоположный

### ⬟ Фиолетовый пентагон-спираль
- **Эффект**: Меняет направление спирали
- **Механика**: 
  - Крутился по часовой → крутится против часовой
  - Крутился против часовой → крутится по часовой
  - `spiralSpeed` меняет знак

### 🔥 Янтарный огненный шар
- **Эффект**: Отскакивает в противоположном направлении
- **Механика**: Меняет вектор скорости, продолжает оставлять след

### 👻 Призрачный шар
- **Эффект**: Отскакивает в противоположном направлении
- **Механика**: Меняет вектор скорости, сохраняет прозрачность

### 🔺 Оранжевый треугольник-спиннер
- **Эффект**: Меняет направление вращения
- **Механика**: 
  - Вращался по часовой → вращается против часовой
  - `rotationSpeed` меняет знак

### 🌀 Фиолетовый спиннер
- **Эффект**: Меняет направление вращения вокруг центра
- **Механика**: 
  - Двигался по часовой → движется против часовой
  - `spinSpeed` меняет знак

## Технические детали

### Функция проверки столкновений
```typescript
function checkEntityCollision(entity1: GameEntity, entity2: GameEntity): boolean {
  const dx = entity1.position.x - entity2.position.x
  const dy = entity1.position.y - entity2.position.y
  const distance = Math.sqrt(dx * dx + dy * dy)
  const minDistance = (entity1.size.width + entity2.size.width) / 2
  
  return distance < minDistance
}
```

### Обработка столкновений
```typescript
function handleEntityCollision(entity1: GameEntity, entity2: GameEntity): [GameEntity, GameEntity] {
  // Вычисляем нормализованный вектор столкновения
  const dx = entity2.position.x - entity1.position.x
  const dy = entity2.position.y - entity1.position.y
  const distance = Math.sqrt(dx * dx + dy * dy)
  const nx = dx / distance
  const ny = dy / distance
  
  // Применяем эффекты в зависимости от типа фигуры
  // ...
}
```

## Примеры взаимодействий

### Круг + Круг
Оба отскакивают в противоположных направлениях

### Пентагон + Огненный шар
- Пентагон меняет направление спирали
- Огненный шар отскакивает

### Треугольник + Спиннер
- Треугольник меняет направление вращения
- Спиннер меняет направление орбиты

### Призрак + Круг
Оба отскакивают в противоположных направлениях

## Игровой эффект

### Увеличение сложности
- Фигуры становятся менее предсказуемыми
- Создаются хаотичные паттерны движения
- Игроку нужно следить за взаимодействиями

### Визуальная динамика
- Игра выглядит более живой и интересной
- Фигуры взаимодействуют друг с другом
- Создается эффект "физического мира"

## Производительность

### Оптимизация
- Проверка O(n²) для всех пар фигур
- Выполняется каждый кадр (60 FPS)
- Эффективно для ~10-20 фигур одновременно

### Ограничения
- Не проверяются статичные объекты
- Простая физика без массы и импульса
- Мгновенное изменение направления

## Будущие улучшения (опционально)
- Добавить массу фигур для реалистичного отскока
- Учитывать импульс при столкновениях
- Добавить звуковые эффекты при столкновениях
- Визуальные эффекты (искры, вспышки)
